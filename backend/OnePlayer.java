package backend;

public class OnePlayer {
    GameEngin g = new GameEngin();
    ObserverGame o = new ObserverGame();

    public OnePlayer() {
        while (!g.gameOver()) {
        }
    }

    private returnX()
}
